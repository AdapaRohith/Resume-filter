# Server Configuration
PORT=5000

# OpenAI API Key (Optional - if not provided, will use keyword-based scoring)
# Get your key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Scoring Mode: 'ai' or 'keyword'
# 'ai' - Uses OpenAI GPT for intelligent scoring (requires API key)
# 'keyword' - Uses keyword matching algorithm (no API key needed)
SCORING_MODE=keyword
